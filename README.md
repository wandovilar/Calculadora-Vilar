# 🧮 Projeto Calculadora Vilar

Este projeto é uma **Calculadora Funcional** desenvolvida com o objetivo principal de consolidar as habilidades de **Front-end** e demonstrar o domínio sobre o sistema de layout **Flexbox** no CSS.

## ✨ Objetivos e Foco do Projeto

O principal objetivo do desenvolvimento inicial foi a **construção do layout (Front-end)**, garantindo que a estrutura visual fosse modular, responsiva e organizada antes de adicionar qualquer lógica de programação.

### Conceitos de CSS Dominados

| Conceito | Aplicação no Projeto |
| :--- | :--- |
| **Flexbox** | Utilizado para criar a estrutura principal da calculadora, empilhando verticalmente o visor e as linhas de botões (`flex-direction: column;`). |
| **Flexbox (Aninhado)** | Aplicado em cada linha de botões (`operadores-conteiner`) para distribuir os elementos de forma uniforme (`justify-content: space-around;` e `gap`). |
| **Box Model** | Controle preciso de margens (`margin`), preenchimentos (`padding`) e dimensões para o ajuste fino do layout. |
| **Pseudo-classes** | Uso do `:hover` e `transform: scale()` para criar efeitos visuais e dar feedback de interação ao usuário nos botões. |
| **Estilização de Texto** | Uso da propriedade `text-align: right` para garantir o alinhamento correto dos números no visor. |
| **Neumorfismo (UI/UX)** | Uso de `box-shadow` e cores neutras para criar o efeito de profundidade e botões "flutuantes". |

---

## ⏭️ Próxima Etapa: O Módulo JavaScript

A calculadora está pronta em sua estrutura visual. O foco agora é a transição para a lógica de programação.

* **Intenção:** Adicionar **JavaScript** para fazer a calculadora funcionar de forma completa, processando as operações de soma, subtração, multiplicação e divisão.
* **Foco no JS:**
    1.  Manipulação do DOM (Document Object Model) para ler e escrever no visor.
    2.  Implementação de **Event Listeners** para cada botão.
    3.  Desenvolvimento da lógica de cálculo.

---

## 🛠️ Tecnologias Utilizadas

* **HTML5** (Estrutura Semântica)
* **CSS3** (Estilização e Layout)
* **Flexbox** (Sistema de Layout Principal)
* **JavaScript** (Próximo Módulo de Implementação)